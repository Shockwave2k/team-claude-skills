# Agents

Custom subagents, organized by domain (`backend/`, `frontend/`, `shared/`, etc.). Each subagent is a single `.md` file with YAML frontmatter.

Start from `templates/AGENT.template.md`. Names must be unique across all categories — the installer flattens them into `~/.claude/agents/`.
